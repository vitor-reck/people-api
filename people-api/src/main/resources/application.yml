spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/people
    username: postgres
    password: postgres@
  jpa:
    hibernate:
      ddl-auto: update
server:
  servlet:
    context-path: /api/v1